# Keep real API keys out of git.
# Add real values in local .env files or deployment environment variables.

VITE_APP_NAME=Coconala Tool
VITE_ENABLE_SAMPLE_DATA=true

# Future server-side only keys. Do not expose production secrets in client code.
EBAY_CLIENT_ID=
EBAY_CLIENT_SECRET=
YAHOO_SHOPPING_APP_ID=
RAKUTEN_APP_ID=
BRAVE_SEARCH_API_KEY=
EXCHANGE_API_KEY=
