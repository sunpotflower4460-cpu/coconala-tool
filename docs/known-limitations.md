# 既知の制限事項

- 公式な仕組み（API）で自動取得するのは楽天市場のみです。メルカリ・ヤフオク・Amazon・eBay・Yahoo!ショッピング等は、
  検索リンクで開いて確認し、「手動で追加」する運用です（自動での大量取得はしません）。
- 楽天市場の実データを使うには、Cloudflare Workers 版で公開し、楽天のアプリIDとアクセスキーをサーバー側に設定する必要があります
  （[`deployment-guide.md`](deployment-guide.md)）。静的版（`app-static/`）・Vercel・GitHub Pages では見本データのみです。
- 楽天の新しい仕組み（2026年〜）に合わせています。楽天側の仕様変更・障害・利用上限の影響を受けることがあります。
  接続できないときは理由を表示して見本データに切り替えます。
- 表示価格・利益見込みは参考値です。在庫・送料・状態は元ページで確認してください。
- 利益計算に取引ごとの固定手数料・消費税・関税は含まれません。手数料は1円未満切り捨てです。
- データ（比較ボード・利益設定・テーマ・履歴）はブラウザ内（localStorage）にだけ保存されます。別の端末・ブラウザとは共有されず、
  ブラウザのデータ削除で消えます。ブラウザの設定で保存が禁止されている場合は、保存せずにその場でのみ使えます。
- 比較ボードは最大50件、リサーチ履歴は最大20件です。
- ログイン・課金・クラウド保存・多言語対応はありません（追加開発の対象）。
- `/api/rakuten` のレート制限（既定: 1IPあたり60秒30回）は Cloudflare の各拠点ごとに数える簡易な仕組みで、厳密な回数管理ではありません。
